# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — never commit secrets
.env
.env.*
!.env.example

# Firebase service account key — contains privileged credentials
service-account.json
*-service-account.json
firebase-adminsdk*.json

# LeaguePals raw API cache — regenerated by `npm run fetch`, never commit
leaguepals-data/

# Weekly standings PDFs — large binaries, stored in Google Drive instead.
# The JSON caches (snapshot-ids.json, drive-uploads.json) ARE committed so
# GitHub Actions can resume without re-generating all snapshot IDs each week.
weekly-standings-pdfs/*.pdf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*.local
